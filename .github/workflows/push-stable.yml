name: Push Stable

on:
  push:
    tags:
      - v1.2.*
      - v1.3.*
      - v1.4.*
  workflow_dispatch:

jobs:
  push-stable:
    uses: anycable/github-actions/.github/workflows/push-stable.yml@master
